{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn chronomail.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/admin/login/",
    "healthcheckTimeout": 300
  }
}
